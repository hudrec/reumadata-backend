[tool.poetry]
name = "reumadata_backend"
version = "0.1.0"
description = ""
authors = ["Hugo del Solar <hudrec11@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.6"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
